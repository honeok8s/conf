services:
  qinglong:
    image: whyour/qinglong:latest
    container_name: qinglong
    hostname: qinglong
    ports:
      - "5700:5700"
    volumes:
      - ./data:/ql/data
    restart: unless-stopped